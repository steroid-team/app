<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>
    <dimen name="nav_header_vertical_spacing">30dp</dimen>
    <dimen name="nav_header_height">250dp</dimen>
    <!-- setPadding accepts a value in px, but the WebView inside the RichEditor already takes
    care of the different screen densities, so just use px instead of dp. -->
    <dimen name="note_body_padding" tools:ignore="PxUsage">26px</dimen>
</resources>